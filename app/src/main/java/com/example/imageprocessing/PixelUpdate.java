package com.example.imageprocessing;

class PixelUpdate {
    int x, y;
    int color;

    PixelUpdate(int x, int y, int color) {
        this.x = x;
        this.y = y;
        this.color = color;
    }
}